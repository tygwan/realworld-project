# Artifacts

Generated reconstruction, model, and simulation artifacts live here.

Suggested subdirectories:

```text
artifacts/reconstruction/
artifacts/segmentation/
artifacts/unity-import/
artifacts/reports/
```

Payloads are ignored by git by default. Record important outputs in
`data/manifests/pipeline-runs.example.csv` or a project-specific manifest.

