# Processed Data

Processed outputs live here when they are useful across pipeline stages.

Payloads are ignored by git by default. If a small processed file should become
versioned evidence, add an explicit exception and record the reason in
`docs/PROJECT-JOURNAL.md`.

