# Interim Data

Local-only intermediate pipeline outputs live here.

Suggested subdirectories:

```text
data/interim/frames/
data/interim/masks/
data/interim/tracks/
data/interim/camera/
```

Payloads are ignored by git because frame extraction and model outputs can be
large.
