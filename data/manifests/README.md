# Data Manifests

Manifests are git-tracked metadata for local-only files.

Use manifests to describe files that should not be committed, such as private
videos, GLB files, schedule CSVs, extracted frames, and reconstruction outputs.
