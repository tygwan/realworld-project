# Captures

Local screenshots, Unity play-mode captures, and recorded demos live here.

Payloads are ignored by git by default. Commit selected small images only after
explicitly deciding they are safe to publish.
